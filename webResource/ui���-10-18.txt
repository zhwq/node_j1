标签超出显示范围时的容错处理
插件尽量有自我保护能力
包含常用的状态，允许某项标签不可用
代码控制标签页面之间的切换
uiBase是build工具根据构建列表从source里提取出来集成包
可以根据需要来配置构建列表，这样就可以提取需要的插件来打包
source里每个每个文件夹都是一个组件或者是资源单位
图片资源建议使用cssSprite方式 拼合图片资源 图片名称原则：插件名_分类（可选）_功能（可选）.png
每个插件建议只使用一张图片资源，复杂的插件也除外，但也要遵循尽力减少图片数量的原则
里面有些原则是任何时候开发都应该遵循的。
希望在开发插件的过程中大家都能够接触到正规软件公司的开发和工作思想
应该说是相对正规一点的公司的一些开发思路
我没有具体规范插件的名称列表和插件包含的方法列表，关键原则是要使插件的使用简单，在可能的情况下使用者可以不用写一行js就可以使用插件运行
允许使用元素data自定义属性
插件初始版本声明为“1.0”
尽量将插件命名空间绑定在jqueryUI之下
一些使用频率较高并且插件名过长的插件可以允许增加一个简化调用方式
注意如果data-appList输入了非定义的参数的处理
data-appList，为了防止重名可以适当加长命名，如data-appListAll=“”
在输入非定义的参数时建议使用两个参数中的默认的一个
注意库中引用的jQueryUI包中只有其核心的扩展，不包含官方开发的jqueryUI插件
避免太简单的命名引起和别人的命名冲突 前提是方便理解和记忆
建议tab类的插件可以有两种处理方式，1、页面中已经存在的div堆栈。2、url引用
你们两个人可以商量一下，尽量保持在调用和命名方面保持一致
为了减少插件的复杂性，方便独立维护
用法指的是例如：每个插件都有一个width参数用来设置宽度，都有一个data-uiType来定义插件的形态
希望这个插件库是可以和使用者产生默契的生命，而不是一个看上去像垃圾的烂摊子
尽量让学习曲线小一些
高级搜索（主框架右上角的搜索），实时搜索（左侧含放大镜图标的文本框），
滑块组件（调整进度百分数），微调组件（数字微调），微型进度条（数据列表中的简化的进度条）
最好不要急于写插件的代码，在写之前一定要想好你要赋予它什么特性、它拥有什么能力，你是这个插件的造物主。
注意插件的自我保护能力，例如在使用者输入无效的属性参数的时候，插件不会因此而崩溃。

QUnit是一个强大的JavaScript单元测试框架，用于调试代码。该框架是由jQuery团队的成员所开发，并且是jQuery的官方测试套件。任意正规JavaScript代码QUnit都能测试。
注意：
1、开发完成的插件可以在本地使用build工具构建之后在WidgetLib下的index.html中测试，但是不允许上传本地构建的uiBase目录；允许在build/buildList.txt中添加已经测试无误的插件（注意格式），允许上传WidgetLib/index.html中的代码（注意保持代码简洁）。
2、构建后的uiBase\skins\default下的css文件中存在已知的问题：文件的代码中包含软换行特殊字符，需要手动删除，这是编写的构建功能代码的缺陷尚未解决。如果没有删除这些特殊字符，将会引起一些插件的样式丢失。
tab在页面宽度过窄的时候会出现显示不正常
第二条中的问题是构建器的bug，和代码本身没有关系。构建器在合并多个文件的时候会在文件之间的内容中插入一个特殊字符
确保项目中文件的纯净
注意脚本的引用和顺序
不过这也是经验，不要一开始就着急编码，想好思路，确认你的思路是相对优化的，验证细节，正式编码
其实两种方法都差不多，只是核心思想不同，一种是直接明显，一种是代码低耦合
不允许直接引用插件的样式文件,引用整合后的这个css ui-all.css
注意排版:
	描述、效果、代码，这三项是平级的
	代码项不隶属与效果项
	效果的展示框内不允许包含代码展示
	注意代码细节
1、确认一下禁用的按钮应该是什么样子，假设灰显应该是正确的
2、禁用的按钮在鼠标移上时才显示正常的效果，最初显示的应该是错误的效果
>>>alert:在使用计时器时会有问题,还会终止一些事件
有些时候只能用原生的事件
jQuery的事件都是代理的方式实现的，类似冒泡和捕获。
有些事件是没有办法处理方法中的中断的>>比较诡异的问题
如果不太了解这些特性，在需要做复杂逻辑的时候建议用原生的,或者在遇到问题的时候尝试用原生的来试试
-------------
非空,则添加、保留标识
不非空
	1.验证合格 去除标识
	2.验证不合格 添加保留标识
-------------
三部负责模块：会议管理 内部论坛 目标计划 工作任务 招聘管理 按钮权限 指标库
浏览器(类别、版本):  IE7-IE9 。FF。safari

只要可以用代码来操作标签切换就可以了
页签现在的处理方式是默认选中第一个；
如果要选中别的，是假设传入的参数是1,2,3或者0,1,2这种格式的，还是添加一个自定义的属性比较合适？
标签的启用禁用肯定也没有做吧？
插件-够用就行，不要太复杂 代码尽量简洁 代码能够看懂，方便以后扩展或者完善


















